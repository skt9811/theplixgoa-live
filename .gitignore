# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.vercel
.vite
.env*
.env.local
.env.*.local
# .env.example holds redacted placeholders only — safe to commit as the
# template other envs are generated from.
!.env.example

# Runtime state for scripts/outreach/ — churns on every real send, not
# source. outreach-tracker.csv is a 1:1 derived view of outreach-db.json
# (regenerate with `npm run outreach:export`), same reasoning.
scripts/outreach/outreach-db.json
scripts/outreach/outreach-tracker.csv

# Android release signing credentials — never committed
android/keystore.properties
keystore.properties
